<h2><%= @place.name %>で</h2>
<h4>あなたの落としたものを以下のジャンルの中から選んでください</h4>
  	 
  	 <table class="item_genre_info has_border">
			<tbody>
				<% @item_genres.each do |item_genre| %>
					<tr>
				<td>
		          <%= link_to step4_path do %>
		            <%= attachment_image_tag item_genre, :genre_image, format: 'png', size:"100x100", fallback: "no_image.png" %>
		          <% end %>
		        </td>
						<td><%= link_to item_genre.name, phase4_path(item_genre_id: item_genre.id, place_id:  @place.id) %></td>
						<td>
						
				<% end %>
			</tbody>
		</table>