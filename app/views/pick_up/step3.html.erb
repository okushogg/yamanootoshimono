<h2><%= @mountain_name.mountain_name %>で</h2>
<h3>あなたの拾ったものを以下のジャンルの中から選んでください</h3>
  	 <table class="item_genre_info has_border">
			<tbody>
				<% @item_genres.each do |item_genre| %>
					<tr>
				<td>
		          <%= link_to step4_path do %>
		            <%= attachment_image_tag item_genre, :genre_image, format: 'jpg', size:"100x100", fallback: "no_image.jpg" %>
		          <% end %>
		        </td>
						<td><%= link_to item_genre.item_genre_name, step4_path(item_genre_id: item_genre.id, mountain_name_id:  @mountain_name.id) %></td>
						<td>
						
				<% end %>
			</tbody>
		</table>